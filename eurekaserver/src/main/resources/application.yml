server:
  port: 8761
eureka:
  client:
    #是否要要注册自己，eureka本身也是一个微服务，设置为false，表我是注册中心，用来注册别的微服务
    register-with-eureka: false
    #是否要同步其他注册中心的数据
    fetch-registry: false
    #访问路径
    service-url:
      defaultZone: http://localhost:8761/eureka/